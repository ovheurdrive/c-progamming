#include <stdlib.h>
#include <stdio.h>

int main(char* argv, int argc)
{
	int a;
	scanf("%d", a); // permet de prendre la valeur de a dans l'entrée standard (stdin) de manière formatée
	printf("%d", a); // affiche la valeur de a dans la sortie standard (stdout) de manière formatée

	return 0;
}
